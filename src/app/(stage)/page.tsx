/**
 * HOME.
 *
 * The frame *is* the home scene — identity, NOVA, her line, the readout — so
 * there's no extra content to place around her.
 */
export default function HomeScene() {
  return null;
}
